using System;
using System.Collections.Generic;
using System.Text;

namespace MovieScanner.Entities
{
    public enum StorageMode
    {
        Unspecified = 0,
        Override = 1,
        Merge = 2
    }
}
